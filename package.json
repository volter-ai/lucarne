{
  "name": "lucarne-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "npm-workspaces root for the lucarne platform (packages/lucarne is the published `lucarne` engine).",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "npm run test --workspaces --if-present && npm run test:docs",
    "test:acceptance": "npm run test:acceptance --workspaces --if-present",
    "test:docker": "npm run test:docker --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present && npm run test:docs",
    "test:docs": "node test/docs-lint.mjs",
    "build:image": "npm run build:image --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
