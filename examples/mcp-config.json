{
  "//": "Point any MCP client (Claude Desktop, etc.) at the lucarne stdio MCP server. Start a daemon first (npx lucarne serve); the MCP server talks to it over LUCARNE_URL so sessions outlive the agent. Tools: lucarne_create, lucarne_list, lucarne_destroy, lucarne_act, lucarne_content.",
  "mcpServers": {
    "lucarne": {
      "command": "npx",
      "args": ["-y", "lucarne-mcp"],
      "env": {
        "LUCARNE_URL": "http://127.0.0.1:7800",
        "LUCARNE_TOKEN": ""
      }
    }
  }
}
